<!DOCTYPE html>
<html>
  <head>
    <style>
      body{
        background-color: #c9cfea;
      }
      #customers {
        font-family: Arial, Helvetica, sans-serif;
        border-collapse: collapse;
        width: 70%;
        transform: translate(-50%,-50%);
        position: absolute;
        top: 50%;
        left: 50%;
      }

      #customers td,
      #customers th {
        border: 1px solid #ddd;
        padding: 8px;
      }

      #customers tr:nth-child(even) {
        background-color: #f2f2f2;
      }

      #customers tr:hover {
        background-color: #ddd;
      }

      #customers th {
        padding-top: 12px;
        padding-bottom: 12px;
        text-align: left;
        background-color: #04aa6d;
        color: white;
      }
    </style>
  </head>
  <body>
    <table id="customers">
      <tr>
        <th>id</th>
        <th>Username</th>
        <th>Name</th>
        <th>Gender</th>
        <th>Phone Number</th>
      </tr>

      <%
        for(var i=0;i<data.length;i++)
        { %>
            <tr>
                <td>
                    <%=i+1%></td>
                 <td>   <%= data[i].username %></td>
                 <td>   <%= data[i].name %></td>
                 <td>   <%= data[i].gender %></td>
                 <td>   <%= data[i].phone %></td>
                 
                 
                 
                 </tr>



       <% }
        %>
    </table>

    <li style="list-style: none; font-size: 30px; float: right; margin-top: 14px;" ><a href=<%=logout %>> <b style="border: 2px dotted black;">Logout</b></a></li>

  </body>
</html>
